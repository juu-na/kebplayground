"""The campus buddy matching pipeline.

The modules are listed below in the order the data moves through them, which
is also the order to write them in:
models -> data -> features -> constraints -> scoring -> matcher -> llm -> cli.
"""
